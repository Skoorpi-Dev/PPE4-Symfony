{% extends 'base.html.twig' %}

{% block title %}Adresse{% endblock %}

{% block body %}
    <h1>Adresse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ adresse.libelle }}</td>
            </tr>
            <tr>
                <th>Adr1</th>
                <td>{{ adresse.adr1 }}</td>
            </tr>
            <tr>
                <th>Adr2</th>
                <td>{{ adresse.adr2 }}</td>
            </tr>
            <tr>
                <th>Defaut</th>
                <td>{{ adresse.defaut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adresse_index') }}">back to list</a>

    <a href="{{ path('adresse_edit', {'id': adresse.id}) }}">edit</a>

    {{ include('adresse/_delete_form.html.twig') }}
{% endblock %}
