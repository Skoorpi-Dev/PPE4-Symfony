{% extends 'base.html.twig' %}

{% block title %}TypeImage{% endblock %}

{% block body %}
    <h1>TypeImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_image.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_image.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_image_index') }}">back to list</a>

    <a href="{{ path('type_image_edit', {'id': type_image.id}) }}">edit</a>

    {{ include('type_image/_delete_form.html.twig') }}
{% endblock %}
