<form method="post" action="{{ path('type_image_delete', {'id': type_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_image.id) }}">
    <button class="btn">Delete</button>
</form>
